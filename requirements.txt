fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# CV + ML
ultralytics>=8.2.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
easyocr>=1.7.0

# HTTP
requests>=2.31.0

# Evidence signing (optional — signing key auto-generated on startup if installed)
cryptography>=42.0.0

# Concept doc generator (optional — only needed to run generate_concept_doc.py)
reportlab>=4.0.0

# Environment variable loading (optional — .env file support)
python-dotenv>=1.0.0
